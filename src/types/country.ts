export type Country = {
  id: string;
  name: string;
  countryCode: string;
  address: string;
  businessNumber: string;
  currency: string;
  createdAt: string;
};

export type CountryDTO = {
  name: string;
  countryCode: string;
  address: string;
  businessNumber: string;
  currency: string;
};
